# Kling API credential: a static API Key from https://kling.ai/dev/api-key
# The library reads KLING_API_KEY from the environment; the CLI also reads ./.env and ~/.kling/.env.
KLING_API_KEY=

# Optional CLI defaults
# KLING_OUTPUT_DIR=./output
