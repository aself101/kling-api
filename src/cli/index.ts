#!/usr/bin/env node
/**
 * `kling` CLI entry — placeholder until Phase 6a₁ (spec D15).
 *
 * The 1.x CLI was removed at 2a₀ with the rest of the JWT-era surface; the 2.0 command
 * tree (`video`, `image`, `elements`, `voices`, `avatar`, `audio`, `tasks`, `account`)
 * lands in `src/cli/` in 6a₁/6a₂. Until then the binary exists so `package.json#bin`
 * resolves, and says so.
 */
console.error('kling 2.0 CLI is under construction on this branch (release/2.0) — see docs/specs/kling-api-2.0-migration-checklist.md, Phase 6a.');
process.exit(1);
